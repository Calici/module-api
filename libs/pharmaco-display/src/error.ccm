module;
#include <string_view>
export module pharmaco.display:error;

namespace pharmaco::display {
  export struct display_error {
    std::string_view msg;
    constexpr const char* what() const noexcept {
      return msg.data();
    }
  };
}